# frozen_string_literal: true

RSpec.describe ISO3166::Countries do
  it "has a version number" do
    expect(ISO3166::Countries::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
